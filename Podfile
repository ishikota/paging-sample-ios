# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'paging-sample-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.1'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'

  # Pods for paging-sample-ios

  target 'paging-sample-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
